#pragma once

// Local-only WiFi credentials. Swap these for whatever network the badge
// should join; kept in its own header so it's obvious what to edit.
#define WIFI_SSID "Elora-House"
#define WIFI_PASS "RogersElora"
